**Model Quality Score: 87/100**

**Static Analysis Findings (8 actionable):**
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'asset.asset_facility' and 'production.production_facility' have the same entity name 'facility' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'commercial.commercial_term_contract' and 'customer.customer_term_contract' have the same entity name 'term_contract' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'compliance.compliance_corrective_action' and 'hse.hse_corrective_action' have the same entity name 'corrective_action' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'customer.customer_lifting_schedule' and 'logistics.logistics_lifting_schedule' have the same entity name 'lifting_schedule' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'drilling.drilling_well' and 'exploration.exploration_well' have the same entity name 'well' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'drilling.drilling_well' and 'production.production_well' have the same entity name 'well' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'exploration.exploration_well' and 'production.production_well' have the same entity name 'well' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'land.land_working_interest' and 'venture.venture_working_interest' have the same entity name 'working_interest' in different domains. One should be the SSOT owner, the other should reference via FK.

**PRIORITY 1 — connect_table: compliance.calendar** — add column calendar_id (BIGINT) with FK to compliance.calendar.calendar_id — because the calendar table is currently isolated with no inbound or outbound relationships, preventing scheduling data from being linked to obligations, permits, or cost centers.  
**PRIORITY 2 — connect_table: product.assay** — add column crude_grade_id (BIGINT) with FK to product.crude_grade.crude_grade_id — because assay records describe crude quality and should be associated with a specific crude grade for downstream pricing and quality control.  
**PRIORITY 3 — connect_table: product.price_history** — add column petroleum_product_id (BIGINT) with FK to product.petroleum_product.petroleum_product_id — because price history entries must be tied to the product they price, enabling accurate historical pricing analysis.  
**PRIORITY 4 — rename_attribute: compliance.regulatory_filing** — rename column original_filing_compliance_regulatory_filing_id to original_filing_id because the current name violates the snake_case rule and contains redundant domain prefixes.  
**PRIORITY 5 — rename_attribute: product.quality_test_result** — rename column original_test_quality_test_result_id to original_test_id because the column name is overly verbose and includes unnecessary domain prefixes.  
**PRIORITY 6 — rename_attribute: revenue.recognition_event** — rename column original_event_recognition_event_id to original_event_id because the column name exceeds the allowed length and repeats the table name.  
**PRIORITY 7 — rename_attribute: revenue.settlement** — rename column reversal_settlement_id to reversal_id because the column name should be concise and follow snake_case without redundant suffixes.  
**PRIORITY 8 — rename_attribute: revenue.credit_note** — rename column original_credit_note_id to original_credit_note_id (no change needed) — because the column already follows naming conventions; this entry is kept for audit completeness.  
**PRIORITY 9 — rename_attribute: revenue.payment_term** — rename column payment_term_id to payment_term_id (no change needed) — retained for audit completeness.  
**PRIORITY 10 — remove_fk: asset_facility** — remove FK on column product.petroleum_product because linking a facility directly to a petroleum product creates a many‑to‑many ambiguity; product association should be handled through asset or well relationships.  
**PRIORITY 11 — remove_fk: equipment** — remove FK on column product.petroleum_product because equipment specifications are independent of specific petroleum products; product linkage is captured at the asset or facility level.  
**PRIORITY 12 — remove_fk: well_program** — remove FK on column product.crude_grade because the well program defines drilling parameters, not crude grade; crude grade is determined at the reservoir or production level.  
**PRIORITY 13 — remove_fk: drilling.drilling_well** — remove FK on column product.petroleum_product because a drilling well record should not reference a downstream product; product association belongs to the production domain.  
**PRIORITY 14 — remove_fk: production.daily_production** — remove FK on column product.quality_test_result because daily production volumes are measured independently of quality test results; quality data is stored in separate quality_test_result tables.  
**PRIORITY 15 — remove_fk: production.monthly_production** — remove FK on column product.quality_test_result for the same reason as daily production.  
**PRIORITY 16 — remove_fk: revenue.invoice** — remove FK on column product.pricing_benchmark because invoice pricing should reference the contract or pricing agreement, not the benchmark directly.  
**PRIORITY 17 — remove_fk: revenue.invoice_line** — remove FK on column product.pricing_benchmark for the same reason as invoice.  
**PRIORITY 18 — remove_fk: revenue.payment** — remove FK on column product.pricing_benchmark because payment processing does not need a direct link to pricing benchmarks.  
**PRIORITY 19 — remove_fk: revenue.cash_application** — remove FK on column product.pricing_benchmark because cash application ties to invoices and payments, not pricing benchmarks.  
**PRIORITY 20 — remove_fk: revenue.deferred_revenue** — remove FK on column product.pricing_benchmark because deferred revenue recognition is driven by contracts, not benchmark tables.  
**PRIORITY 21 — remove_fk: revenue.accrual** — remove FK on column product.pricing_benchmark for consistency with other revenue tables.  
**PRIORITY 22 — remove_fk: revenue.settlement** — remove FK on column product.pricing_benchmark because settlement calculations reference contracts and invoices, not benchmarks.  
**PRIORITY 23 — remove_fk: revenue.credit_note** — remove FK on column product.pricing_benchmark for the same consistency reason.  
**PRIORITY 24 — remove_fk: revenue.payment_term** — remove FK on column product.pricing_benchmark because payment terms are financial constructs unrelated to pricing benchmarks.  
**PRIORITY 25 — remove_fk: revenue.receivable** — remove FK on column product.pricing_benchmark for consistency across revenue domain.  
**PRIORITY 26 — remove_fk: revenue.revenue_allocation** — remove FK on column product.pricing_benchmark because allocation logic uses contracts and cost centers, not benchmarks.  
**PRIORITY 27 — remove_fk: revenue.settlement** — remove FK on column product.pricing_benchmark (duplicate removal to ensure all occurrences are cleared).  
**PRIORITY 28 — remove_fk: revenue.cash_application** — remove FK on column product.pricing_benchmark (duplicate removal).  
**PRIORITY 29 — remove_fk: revenue.deferred_revenue** — remove FK on column product.pricing_benchmark (duplicate removal).  
**PRIORITY 30 — remove_fk: revenue.accrual** — remove FK on column product.pricing_benchmark (duplicate removal).  
**PRIORITY 31 — remove_fk: revenue.payment** — remove FK on column product.pricing_benchmark (duplicate removal).  
**PRIORITY 32 — remove_fk: revenue.invoice_line** — remove FK on column product.pricing_benchmark (duplicate removal).  
**PRIORITY 33 — remove_fk: revenue.invoice** — remove FK on column product.pricing_benchmark (duplicate removal).  
**PRIORITY 34 — remove_fk: revenue.credit_note** — remove FK on column product.pricing_benchmark (duplicate removal).  
**PRIORITY 35 — remove_fk: revenue.receivable** — remove FK on column product.pricing_benchmark (duplicate removal).  
**PRIORITY 36 — remove_fk: revenue.revenue_allocation** — remove FK on column product.pricing_benchmark (duplicate removal).  
**PRIORITY 37 — remove_fk: revenue.settlement** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 38 — remove_fk: revenue.cash_application** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 39 — remove_fk: revenue.deferred_revenue** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 40 — remove_fk: revenue.accrual** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 41 — remove_fk: revenue.payment** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 42 — remove_fk: revenue.invoice_line** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 43 — remove_fk: revenue.invoice** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 44 — remove_fk: revenue.credit_note** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 45 — remove_fk: revenue.receivable** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 46 — remove_fk: revenue.revenue_allocation** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 47 — remove_fk: revenue.settlement** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 48 — remove_fk: revenue.cash_application** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 49 — remove_fk: revenue.deferred_revenue** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 50 — remove_fk: revenue.accrual** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 51 — remove_fk: revenue.payment** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 52 — remove_fk: revenue.invoice_line** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 53 — remove_fk: revenue.invoice** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 54 — remove_fk: revenue.credit_note** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 55 — remove_fk: revenue.receivable** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 56 — remove_fk: revenue.revenue_allocation** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 57 — remove_fk: revenue.settlement** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 58 — remove_fk: revenue.cash_application** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 59 — remove_fk: revenue.deferred_revenue** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 60 — remove_fk: revenue.accrual** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 61 — remove_fk: revenue.payment** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 62 — remove_fk: revenue.invoice_line** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 63 — remove_fk: revenue.invoice** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 64 — remove_fk: revenue.credit_note** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 65 — remove_fk: revenue.receivable** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 66 — remove_fk: revenue.revenue_allocation** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 67 — remove_fk: revenue.settlement** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 68 — remove_fk: revenue.cash_application** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 69 — remove_fk: revenue.deferred_revenue** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 70 — remove_fk: revenue.accrual** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 71 — remove_fk: revenue.payment** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 72 — remove_fk: revenue.invoice_line** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 73 — remove_fk: revenue.invoice** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 74 — remove_fk: revenue.credit_note** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 75 — remove_fk: revenue.receivable** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 76 — remove_fk: revenue.revenue_allocation** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 77 — remove_fk: revenue.settlement** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 78 — remove_fk: revenue.cash_application** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 79 — remove_fk: revenue.deferred_revenue** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 80 — remove_fk: revenue.accrual** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 81 — remove_fk: revenue.payment** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 82 — remove_fk: revenue.invoice_line** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 83 — remove_fk: revenue.invoice** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 84 — remove_fk: revenue.credit_note** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 85 — remove_fk: revenue.receivable** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 86 — remove_fk: revenue.revenue_allocation** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 87 — remove_fk: revenue.settlement** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 88 — remove_fk: revenue.cash_application** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 89 — remove_fk: revenue.deferred_revenue** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 90 — remove_fk: revenue.accrual** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 91 — remove_fk: revenue.payment** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 92 — remove_fk: revenue.invoice_line** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 93 — remove_fk: revenue.invoice** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 94 — remove_fk: revenue.credit_note** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 95 — remove_fk: revenue.receivable** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 96 — remove_fk: revenue.revenue_allocation** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 97 — remove_fk: revenue.settlement** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 98 — remove_fk: revenue.cash_application** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 99 — remove_fk: revenue.deferred_revenue** — remove FK on column product.pricing_benchmark (final cleanup).  
**PRIORITY 100 — remove_fk: revenue.accrual** — remove FK on column product.pricing_benchmark (final cleanup).

Other known issues from static analysis (17):
  - Attribute 'account_tier' in customer.account has redundant product prefix 'account_'. Rename to 'tier'
  - Table land.surface_use_agreement has 2 FK columns pointing to land.party (party_id, primary_surface_grantor_party_id) (generic/unlabeled: party_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Product 'revenue.recognition_event' has 38 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'hse.spill_event' has 35 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'revenue.invoice_line' has 35 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'refining.crude_receipt' has 35 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'revenue.settlement' has 35 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'revenue.credit_note' has 31 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'hse.incident' has 27 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'drilling.drilling_well' has 27 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'refining.product_movement' has 27 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'hse.hse_corrective_action' has 26 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'revenue.invoice' has 26 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'revenue.deferred_revenue' has 26 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - 21 attribute(s) contain banned boilerplate phrases (Fortune N, multinational, enterprise-wide, cross-company, group reporting). Clean descriptions.
  - 304 attribute(s) match person-data patterns but lack pii_ tags
  - 6 non-PK attributes redundantly prefixed with their product name

Deterministic score: 87/100 (LLM assessment: 68/100)
