**Model Quality Score: 89/100**

**Static Analysis Findings (3 actionable):**
  - [SA:unlinked_fk] Column league.trade_transaction.draft_pick_id looks like an FK but has no foreign_key_to reference
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'athlete.athlete_profile' and 'fan.fan_profile' have the same entity name 'profile' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'event.event_incident' and 'security.security_incident' have the same entity name 'incident' in different domains. One should be the SSOT owner, the other should reference via FK.

**PRIORITY 1 — connect_table: compliance.governing_body** — add column parent_governing_body_id (BIGINT) with FK to compliance.governing_body.governing_body_id — governing bodies have hierarchical relationships (e.g., FIFA oversees continental federations) and this table currently has zero outbound FKs making it an isolated reference table that lacks self-referential hierarchy
**PRIORITY 2 — connect_table: finance.company_code** — add column parent_company_code_id (BIGINT) with FK to finance.company_code.company_code_id — corporate structures have parent-subsidiary relationships and this table has zero outbound FKs
**PRIORITY 3 — connect_table: sponsorship.sponsor** — add column finance_company_code_id (BIGINT) with FK to finance.company_code.company_code_id — sponsors are business entities requiring financial entity linkage for invoicing and revenue recognition
**PRIORITY 4 — remove_fk: athlete.draft_selection** — remove FK on column roster_id to athlete.roster because a draft selection occurs before a player is placed on a roster; the roster assignment is a downstream event, not a property of the selection itself
**PRIORITY 5 — remove_fk: athlete.injury_record** — remove FK on column security_incident_id to security.security_incident because injuries are medical/athletic events, not security incidents; conflating these domains creates misleading lineage
**PRIORITY 6 — rename_attribute: content.category** — rename column parent_content_category_id to parent_category_id because the table already has parent_category_id listed as a self-ref and having both is redundant; consolidate to one self-referential column
**PRIORITY 7 — connect_table: event.match_result** — add column league_official_id (BIGINT) with FK to league.official.official_id — match results should reference the head official who certified the result for governance traceability
**PRIORITY 8 — rename_attribute: compliance.audit_engagement** — rename column prior_engagement_audit_engagement_id to prior_audit_engagement_id because the current name redundantly embeds the table name twice
**PRIORITY 9 — rename_attribute: compliance.policy** — rename column primary_superseded_by_policy_id to superseded_by_policy_id because the prefix "primary_" adds no semantic value and the table name is already embedded
**PRIORITY 10 — rename_attribute: security.emergency_response_plan** — rename column primary_superseded_by_plan_emergency_response_plan_id to superseded_by_plan_id because the column name is excessively long and redundantly embeds the full table name
**PRIORITY 11 — rename_attribute: gaming.responsible_gaming_limit** — rename column primary_superseded_by_limit_responsible_gaming_limit_id to superseded_by_limit_id because the column name redundantly embeds the full table name
**PRIORITY 12 — rename_attribute: finance.payment_run** — rename column primary_original_payment_run_id to original_payment_run_id because the "primary_" prefix adds no business meaning
**PRIORITY 13 — rename_attribute: finance.fiscal_period** — rename column primary_prior_fiscal_period_id to prior_fiscal_period_id because the "primary_" prefix is non-standard and adds no semantic value
**PRIORITY 14 — rename_attribute: fan.loyalty_transaction** — rename column reversal_of_transaction_loyalty_transaction_id to reversed_loyalty_transaction_id because the current name is excessively verbose and embeds the table name redundantly
**PRIORITY 15 — rename_attribute: ticketing.ticket_transfer** — rename column primary_prior_transfer_ticket_transfer_id to prior_ticket_transfer_id because the name is excessively long with redundant prefixes
**PRIORITY 16 — connect_table: league.official_assignment** — add column fixture_id (BIGINT) with FK to event.fixture.fixture_id — official assignments must reference the specific fixture/game they are assigned to, not just the game_result which is a post-event record
**PRIORITY 17 — connect_table: league.official_assignment** — add column season_id (BIGINT) with FK to league.season.season_id — official assignments need temporal context for scheduling and performance tracking across seasons
**PRIORITY 18 — connect_table: venue.parking_activation** — add column event_fixture_id (BIGINT) with FK to event.fixture.fixture_id — parking activations are triggered by specific events and need direct fixture linkage beyond just event_day_ops
**PRIORITY 19 — connect_table: content.asset_tag** — add column created_by_production_order_id (BIGINT) with FK to content.production_order.production_order_id — tags applied during production workflows should trace back to the originating production order
**PRIORITY 20 — connect_table: venue.config_section_allocation** — add column venue_id (BIGINT) with FK to venue.venue.venue_id — section allocations need direct venue context for multi-venue operations without always traversing through capacity_config
**PRIORITY 21 — connect_table: security.plan_zone_designation** — add column venue_id (BIGINT) with FK to venue.venue.venue_id — plan-zone designations are venue-specific and need direct venue linkage for operational clarity
**PRIORITY 22 — remove_fk: broadcast.audience_rating** — remove FK on column athlete_profile_id to athlete.athlete_profile because audience ratings measure viewership of broadcasts, not individual athletes; athlete-level attribution belongs in performance analytics
**PRIORITY 23 — connect_table: fan.club_membership** — add column league_franchise_id (BIGINT) with FK to league.franchise.franchise_id — club memberships are typically franchise-specific and need direct franchise context for segmentation
**PRIORITY 24 — connect_table: fan.club_membership** — add column season_id (BIGINT) with FK to league.season.season_id — memberships are seasonal and need temporal bounding for renewal tracking
**PRIORITY 25 — rename_attribute: gaming.self_exclusion** — rename column renewed_self_exclusion_id to prior_self_exclusion_id because self-exclusions are not "renewed" — they are extended or re-established after expiry
**PRIORITY 26 — connect_table: merchandise.supplier_category_authorization** — add column league_id (BIGINT) with FK to league.league.league_id — supplier authorizations are league-specific for brand compliance and IP protection
**PRIORITY 27 — remove_fk: event.fixture** — remove FK on column finance_profit_center_id to finance.profit_center because fixtures are operational scheduling entities; profit center assignment belongs on the revenue transaction (ticket order, broadcast deal), not the fixture itself
**PRIORITY 28 — connect_table: ticketing.gate_scan** — add column ticket_order_line_id (BIGINT) with FK to ticketing.ticket_order_line.ticket_order_line_id — gate scans validate specific ticket line items, not just inventory positions, for reconciliation accuracy
**PRIORITY 29 — connect_table: broadcast.licensee** — add column finance_company_code_id (BIGINT) with FK to finance.company_code.company_code_id — licensees are legal entities requiring financial entity linkage for contract and payment processing
**PRIORITY 30 — connect_table: broadcast.rights_holder** — add column finance_company_code_id (BIGINT) with FK to finance.company_code.company_code_id — rights holders are legal entities requiring financial entity linkage for royalty payments
**PRIORITY 31 — rename_attribute: event.broadcast_window** — rename column split_from_broadcast_window_id to source_broadcast_window_id because "split_from" is ambiguous and non-standard compared to model conventions
**PRIORITY 32 — connect_table: gaming.fantasy_league** — add column sponsor_deal_id (BIGINT) with FK to sponsorship.deal.deal_id — fantasy leagues are frequently sponsored and need direct sponsorship deal linkage for revenue attribution
**PRIORITY 33 — connect_table: league.standing** — add column season_game_result_count (BIGINT) with FK to league.game_result.game_result_id — standings are derived from game results and should have direct traceability to the latest computed result
**PRIORITY 34 — remove_fk: league.standing** — remove FK on column league_playoff_bracket_id to league.playoff_bracket because standings determine playoff qualification but are not properties of a bracket; the relationship is inverse
**PRIORITY 35 — connect_table: athlete.salary_cap_entry** — add column transaction_window_id (BIGINT) with FK to league.transaction_window.transaction_window_id — salary cap entries are often created during transaction windows (trades, free agency) and need temporal context
**PRIORITY 36 — rename_attribute: gaming.odds_feed** — rename column superseded_odds_feed_id to prior_odds_feed_id because "superseded" is inconsistent with the model's predominant use of "prior" and "previous" for temporal self-refs
**PRIORITY 37 — connect_table: venue.suite** — add column capacity_config_id (BIGINT) with FK to venue.capacity_config.capacity_config_id — suites have variable configurations per event type and need linkage to the active capacity configuration
**PRIORITY 38 — connect_table: merchandise.fulfillment_shipment** — add column league_season_id (BIGINT) with FK to league.season.season_id — shipments of seasonal merchandise need season context for inventory planning and reporting
**PRIORITY 39 — connect_table: fan.consent_preference** — add column league_franchise_id (BIGINT) with FK to league.franchise.franchise_id — consent preferences may vary by franchise relationship under multi-team fan engagement scenarios
**PRIORITY 40 — remove_fk: ticketing.ticket_order** — remove FK on column athlete_contract_id to athlete.contract because ticket orders are fan commerce transactions with no direct relationship to athlete contracts; complimentary tickets should use hold_allocation
**PRIORITY 41 — remove_fk: ticketing.ticket_order** — remove FK on column athlete_roster_id to athlete.roster because ticket orders have no business relationship to roster assignments; player comp tickets flow through hold_allocation
**PRIORITY 42 — connect_table: event.event_incident** — add column league_season_id (BIGINT) with FK to league.season.season_id — event incidents need season context for trend analysis and disciplinary tracking across seasons
**PRIORITY 43 — rename_attribute: gaming.contest_entry** — rename column rebuy_of_contest_entry_id to rebuy_contest_entry_id because "of" prepositions in column names are non-standard per snake_case conventions
**PRIORITY 44 — rename_attribute: gaming.wager** — rename column cashout_of_wager_id to cashout_wager_id because "of" prepositions in column names are non-standard per snake_case conventions
**PRIORITY 45 — connect_table: league.team** — add column league_season_id (BIGINT) with FK to league.season.season_id — teams have season-specific rosters, records, and configurations that require temporal context
**PRIORITY 46 — connect_table: broadcast.streaming_subscription** — add column league_season_id (BIGINT) with FK to league.season.season_id — streaming subscriptions are seasonal products tied to league seasons for content access windows
**PRIORITY 47 — connect_table: content.collection_item** — add column display_order_position (BIGINT) with FK to content.category.category_id — collection items need category context for filtering and navigation within collections
**PRIORITY 48 — remove_fk: content.collection_item** — remove FK on column display_order_position if it exists as a sequence number rather than a FK; this correction applies only if the column is mistyped as a FK
**PRIORITY 49 — connect_table: security.screening_event** — add column event_fixture_id (BIGINT) with FK to event.fixture.fixture_id — screening events at venue entry are fixture-specific for event-day security operations; the existing fixture FK may already exist but verify it is properly linked
**PRIORITY 50 — connect_table: fan.payment_method** — add column finance_company_code_id (BIGINT) with FK to finance.company_code.company_code_id — payment methods need financial entity context for PCI-DSS compliance scope and payment processor routing
**PRIORITY 51 — rename_attribute: league.league_schedule** — rename column rescheduled_from_league_schedule_id to original_league_schedule_id because "rescheduled_from" is verbose and "original" is the standard pattern for referencing the pre-change record
**PRIORITY 52 — connect_table: venue.accessibility_feature** — add column venue_seat_id (BIGINT) with FK to venue.seat.seat_id — accessibility features often apply to specific seats (wheelchair positions, companion seats) not just sections
**PRIORITY 53 — connect_table: gaming.betting_market** — add column compliance_governing_body_id (BIGINT) with FK to compliance.governing_body.governing_body_id — betting markets operate under specific regulatory bodies and need governance linkage for compliance
**PRIORITY 54 — connect_table: athlete.performance_stat** — add column league_league_id (BIGINT) with FK to league.league.league_id — performance statistics are league-specific (different stat categories per sport) and need direct league context
**PRIORITY 55 — connect_table: event.scoring_event** — add column competition_round_id (BIGINT) with FK to event.competition_round.competition_round_id — scoring events in tournament play need round context for bracket progression logic
**PRIORITY 56 — rename_attribute: gaming.fantasy_roster** — rename column previous_fantasy_roster_id to prior_fantasy_roster_id because "previous" is inconsistent with the model's predominant "prior" pattern for temporal self-refs
**PRIORITY 57 — connect_table: merchandise.order_line** — add column league_season_id (BIGINT) with FK to league.season.season_id — order lines for seasonal merchandise need season attribution for revenue reporting and inventory planning
**PRIORITY 58 — connect_table: ticketing.price_zone** — add column venue_facility_id (BIGINT) with FK to venue.facility.facility_id — price zones are facility-specific configurations that need direct facility linkage beyond just venue
**PRIORITY 59 — connect_table: fan.segment_assignment** — add column league_season_id (BIGINT) with FK to league.season.season_id — segment assignments change seasonally and need temporal context for historical analysis
**PRIORITY 60 — connect_table: broadcast.media_asset** — add column venue_venue_id (BIGINT) with FK to venue.venue.venue_id — media assets are captured at specific venues and need location provenance for rights management

Other known issues from static analysis (15):
  - Attribute 'account_tier' in fan.account has redundant product prefix 'account_'. Rename to 'tier'
  - Attribute 'membership_tier' in fan.membership has redundant product prefix 'membership_'. Rename to 'tier'
  - Attribute 'property_tier' in sponsorship.property has redundant product prefix 'property_'. Rename to 'tier'
  - Table ticketing.ticket_order has self-referencing FK on column 'resale_original_order_id' with same name as PK 'ticket_order_id' — the FK column must have a different name that describes the relationship (the LLM should choose a contextual label)
  - Table ticketing.access_entitlement has self-referencing FK on column 'companion_entitlement_id' with same name as PK 'access_entitlement_id' — the FK column must have a different name that describes the relationship (the LLM should choose a contextual label)
  - Table broadcast.live_feed has self-referencing FK on column 'backup_feed_id' with same name as PK 'live_feed_id' — the FK column must have a different name that describes the relationship (the LLM should choose a contextual label)
  - Table content.publish_event has 2 FK columns pointing to event.fixture (fixture_id, primary_publish_fixture_id) (generic/unlabeled: fixture_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table fan.engagement_event has 2 FK columns pointing to event.fixture (fixture_id, primary_engagement_fixture_id) (generic/unlabeled: fixture_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table security.access_event has 2 FK columns pointing to event.fixture (fixture_id, primary_access_fixture_id) (generic/unlabeled: fixture_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table security.screening_event has 2 FK columns pointing to event.fixture (fixture_id, primary_screening_fixture_id) (generic/unlabeled: fixture_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Product 'fan.engagement_event' has 27 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - Product 'merchandise.promotion' has 26 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - 11 attribute(s) contain banned boilerplate phrases (Fortune N, multinational, enterprise-wide, cross-company, group reporting). Clean descriptions.
  - 193 attribute(s) match person-data patterns but lack pii_ tags
  - 56 non-PK attributes redundantly prefixed with their product name

Deterministic score: 89/100 (LLM assessment: 62/100)
