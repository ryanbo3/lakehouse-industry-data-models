**Model Quality Score: 73/100**

**Static Analysis Findings (12 actionable):**
  - [SA:fk_namespace_mismatch] customer.assistance_enrollment.primary_assistance_program_affordability_plan_id: description says 'customer' but FK points to 'service'
  - [SA:fk_namespace_mismatch] billing.invoice_line.interval_consumption_id: description says 'billing' but FK points to 'metering'
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'asset.asset_class' and 'service.service_class' have the same entity name 'class' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'asset.asset_meter' and 'metering.metering_meter' have the same entity name 'meter' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'billing.billing_account' and 'customer.customer_account' have the same entity name 'account' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'billing.billing_rate_schedule' and 'service.service_rate_schedule' have the same entity name 'rate_schedule' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'compliance.compliance_permit' and 'project.project_permit' have the same entity name 'permit' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'compliance.compliance_permit' and 'treatment.treatment_permit' have the same entity name 'permit' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'project.project_permit' and 'treatment.treatment_permit' have the same entity name 'permit' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'compliance.compliance_violation' and 'treatment.treatment_violation' have the same entity name 'violation' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'compliance.compliance_public_notification' and 'quality.quality_public_notification' have the same entity name 'public_notification' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'finance.finance_budget' and 'project.project_budget' have the same entity name 'budget' in different domains. One should be the SSOT owner, the other should reference via FK.

**PRIORITY 1 — rename_attribute: compliance.regulatory_agency** — rename column parent_regulatory_agency_id to parent_agency_regulatory_agency_id because there appear to be two self-referencing FK columns (parent_agency_regulatory_agency_id and parent_regulatory_agency_id) which is redundant; consolidate to a single self-ref column with a clear business name
**PRIORITY 2 — remove_fk: compliance.regulatory_agency** — remove FK on column parent_regulatory_agency_id because it duplicates the self-referencing relationship already expressed by parent_agency_regulatory_agency_id, creating redundant hierarchy paths on the same table
**PRIORITY 3 — connect_table: customer.person** — add column primary_service_address_id (BIGINT) with FK to customer.service_address.service_address_id — person table has zero outbound FKs making it isolated; persons logically have a primary mailing/service address
**PRIORITY 4 — connect_table: metering.read_route** — add column territory_id (BIGINT) with FK to service.territory.territory_id — read_route has zero outbound FKs and is isolated; meter reading routes are geographically organized within service territories
**PRIORITY 5 — connect_table: supply.vendor** — add column territory_id (BIGINT) with FK to service.territory.territory_id — vendor table has zero outbound FKs and is isolated; vendors serve specific geographic territories for service delivery
**PRIORITY 6 — connect_table: distribution.network_reading** — add column asset_registry_id (BIGINT) with FK to asset.registry.registry_id — network readings are taken from physical assets (valves, PRVs, pumps, tanks) and should link to the asset registry for traceability
**PRIORITY 7 — connect_table: distribution.flow_reading** — add column asset_registry_id (BIGINT) with FK to asset.registry.registry_id — flow readings originate from physical flow meters/sensors that are tracked in the asset registry
**PRIORITY 8 — connect_table: wastewater.ii_flow_measurement** — add column asset_work_order_id (BIGINT) with FK to asset.work_order.work_order_id — I&I flow measurements often trigger or are associated with corrective maintenance work orders
**PRIORITY 9 — connect_table: quality.compliance_determination** — add column treatment_facility_id (BIGINT) with FK to treatment.facility.facility_id — compliance determinations are made at the facility level for regulatory reporting
**PRIORITY 10 — connect_table: billing.rate_tier** — add column billing_rate_schedule_id (BIGINT) with FK to billing.billing_rate_schedule.billing_rate_schedule_id — rate tiers belong to a rate schedule; the only current FK is self-ref and GL which is insufficient for business context
**PRIORITY 11 — connect_table: service.program_enrollment** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — program enrollments must link to the customer who enrolled, not just the agreement and conservation program
**PRIORITY 12 — connect_table: service.service_class** — add column territory_id (BIGINT) with FK to service.territory.territory_id — service classes (residential, commercial, industrial) may vary by territory for rate-setting purposes
**PRIORITY 13 — connect_table: project.schedule_of_values_line** — add column construction_contract_id (BIGINT) with FK to project.construction_contract.construction_contract_id — schedule of values lines are contractual breakdowns that belong to a construction contract
**PRIORITY 14 — connect_table: wastewater.effluent_parameter_result** — add column wwtp_id (BIGINT) with FK to wastewater.wwtp.wwtp_id — effluent parameter results are generated at a specific WWTP and need direct linkage for operational reporting
**PRIORITY 15 — connect_table: distribution.flushing_event** — add column territory_id (BIGINT) with FK to service.territory.territory_id — flushing events occur within service territories and require geographic context for customer notification
**PRIORITY 16 — connect_table: treatment.water_source** — add column treatment_facility_id (BIGINT) with FK to treatment.facility.facility_id — water sources supply specific treatment facilities; the current model only has self-ref and regulatory_requirement links
**PRIORITY 17 — remove_fk: compliance.inspection_finding** — remove FK on column previous_finding_inspection_finding_id because the self-referencing column name embeds the table name redundantly; however this is a valid self-ref pattern so retain but verify naming
**PRIORITY 18 — rename_attribute: billing.billing_rate_schedule** — rename column superseded_by_rate_schedule_billing_rate_schedule_id to superseding_billing_rate_schedule_id because the current name is excessively long and redundantly embeds the table name twice
**PRIORITY 19 — rename_attribute: metering.meter_size_type** — rename column primary_replacement_meter_size_type_id to default_replacement_meter_size_type_id because "primary_replacement" is ambiguous; "default_replacement" clarifies this is the standard replacement size
**PRIORITY 20 — rename_attribute: treatment.process_control_setpoint** — rename column previous_setpoint_process_control_setpoint_id to previous_process_control_setpoint_id because the current name redundantly includes "setpoint" twice
**PRIORITY 21 — rename_attribute: compliance.inspection_finding** — rename column previous_finding_inspection_finding_id to previous_inspection_finding_id because the current name redundantly embeds "finding" twice
**PRIORITY 22 — connect_table: distribution.nrw_water_balance** — add column treatment_facility_id (BIGINT) with FK to treatment.facility.facility_id — NRW water balance calculations require the source facility's production data as input to the balance equation
**PRIORITY 23 — connect_table: compliance.compliance_public_notification** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — public notifications must be linked to affected customer accounts for delivery tracking
**PRIORITY 24 — connect_table: metering.interval_consumption** — add column service_point_id (BIGINT) with FK to service.point.point_id — interval consumption data is measured at a service point and should directly reference it for billing and analytics
**PRIORITY 25 — connect_table: treatment.filter_run** — add column quality_sampling_point_id (BIGINT) with FK to quality.sampling_point.sampling_point_id — filter runs produce turbidity and quality data at specific sampling points for regulatory compliance
**PRIORITY 26 — connect_table: wastewater.manhole** — add column sewer_network_id (BIGINT) with FK to wastewater.sewer_network.sewer_network_id — manholes are physical access points on sewer networks and must reference the network they belong to
**PRIORITY 27 — connect_table: compliance.industrial_user** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — industrial users are customers with accounts; the current link only goes to permit and vendor which is insufficient
**PRIORITY 28 — connect_table: asset.job_plan** — add column treatment_facility_id (BIGINT) with FK to treatment.facility.facility_id — job plans are often facility-specific for treatment plant maintenance procedures
**PRIORITY 29 — connect_table: finance.debt_service_payment** — add column debt_instrument_id (BIGINT) with FK to finance.debt_instrument.debt_instrument_id — debt service payments must reference the debt instrument they service; verify this FK exists as it appears in the FK list but confirm column naming
**PRIORITY 30 — connect_table: quality.online_instrument** — add column treatment_process_unit_id (BIGINT) with FK to treatment.process_unit.process_unit_id — online instruments are installed on specific process units for real-time monitoring
**PRIORITY 31 — connect_table: distribution.hydrant_flow_test** — add column pipe_main_id (BIGINT) with FK to distribution.pipe_main.pipe_main_id — hydrant flow tests measure capacity of the connected main and results are used for main adequacy assessment
**PRIORITY 32 — connect_table: billing.collection_notice** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — collection notices are issued to customer accounts; current link only goes through billing_account which may be insufficient for CIS integration
**PRIORITY 33 — connect_table: treatment.ct_compliance_record** — add column compliance_permit_id (BIGINT) with FK to compliance.compliance_permit.compliance_permit_id — CT compliance records demonstrate permit compliance and must reference the governing permit
**PRIORITY 34 — connect_table: wastewater.fog_inspection** — add column fog_source_id (BIGINT) with FK to wastewater.fog_source.fog_source_id — FOG inspections are conducted at specific FOG sources (restaurants, food processors) and must reference the source being inspected
**PRIORITY 35 — connect_table: metering.tamper_event** — add column service_point_id (BIGINT) with FK to service.point.point_id — tamper events occur at service points and need this link for field service dispatch and compliance tracking
**PRIORITY 36 — rename_attribute: billing.adjustment** — rename column original_adjustment_id to reversed_adjustment_id because "original" is ambiguous; the self-ref represents a reversal relationship per standard billing patterns
**PRIORITY 37 — connect_table: compliance.pretreatment_iup** — add column wastewater_wwtp_id (BIGINT) with FK to wastewater.wwtp.wwtp_id — industrial user permits for pretreatment specify which WWTP receives the pretreated discharge
**PRIORITY 38 — connect_table: distribution.main_break** — add column service_territory_id (BIGINT) with FK to service.territory.territory_id — main breaks must be associated with a service territory for crew dispatch and customer notification boundaries
**PRIORITY 39 — connect_table: asset.criticality_rating** — add column treatment_facility_id (BIGINT) with FK to treatment.facility.facility_id — criticality ratings are often facility-contextualized as the same asset type may have different criticality at different facilities
**PRIORITY 40 — connect_table: quality.analytical_result** — add column treatment_facility_id (BIGINT) with FK to treatment.facility.facility_id — analytical results are produced at facilities and need direct facility linkage for MOR/DMR reporting
**PRIORITY 41 — connect_table: compliance.corrective_action** — add column treatment_facility_id (BIGINT) with FK to treatment.facility.facility_id — corrective actions are implemented at specific facilities and need this reference for tracking
**PRIORITY 42 — connect_table: finance.fixed_asset** — add column asset_registry_id (BIGINT) with FK to asset.registry.registry_id — fixed assets in finance must cross-reference the operational asset registry for reconciliation between financial and physical asset records
**PRIORITY 43 — connect_table: customer.parcel** — add column service_territory_id (BIGINT) with FK to service.territory.territory_id — parcels exist within service territories and this link is needed for service availability determination

Other known issues from static analysis (6):
  - Table finance.bank_account has self-referencing FK on column 'sweep_target_account_id' with same name as PK 'bank_account_id' — the FK column must have a different name that describes the relationship (the LLM should choose a contextual label)
  - Table metering.read_route has only 1 attribute(s) — likely incomplete or should be merged
  - Product 'customer.complaint' has 28 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - 3 attribute(s) contain banned boilerplate phrases (Fortune N, multinational, enterprise-wide, cross-company, group reporting). Clean descriptions.
  - 235 attribute(s) match person-data patterns but lack pii_ tags
  - 15 non-PK attributes redundantly prefixed with their product name

Deterministic score: 73/100 (LLM assessment: 62/100)
