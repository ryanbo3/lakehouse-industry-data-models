**Model Quality Score: 93/100**

**PRIORITY 1 — remove_fk: ancillary.seat_assignment** — remove FK on column `maintenance.mel_deferral` because a seat assignment should not directly reference a MEL deferral; the relationship is indirect through aircraft serviceability and creates a nonsensical direct dependency between passenger seating and maintenance deferrals.
**PRIORITY 2 — remove_fk: passenger.accessibility_profile** — remove FK on column referencing `maintenance.mel_item` because passenger accessibility profiles should not directly reference MEL items; accessibility needs are passenger attributes independent of aircraft maintenance deferrals.
**PRIORITY 3 — remove_fk: passenger.ssr_record** — remove FK on column referencing `maintenance.mel_item` because SSR records capture passenger service requests and should not directly link to minimum equipment list items.
**PRIORITY 4 — remove_fk: ancillary.eligibility_rule** — remove FK on column referencing `maintenance.mel_item` because ancillary product eligibility rules should not depend on specific MEL items; this conflates product sales logic with airworthiness deferrals.
**PRIORITY 5 — remove_fk: revenue.ticket_coupon** — remove FK on column referencing `maintenance.technical_log` because ticket coupons are revenue accounting documents and should not directly reference maintenance technical logs.
**PRIORITY 6 — remove_fk: revenue.ticket_coupon** — remove FK on column referencing `maintenance.work_order` because ticket coupons track fare usage, not maintenance activities; any IROP rebooking link should go through flight.irop_event.
**PRIORITY 7 — remove_fk: revenue.recognition** — remove FK on column referencing `maintenance.technical_log` because revenue recognition entries should not directly reference maintenance logs; the relationship should be mediated through flight operations.
**PRIORITY 8 — remove_fk: revenue.refund** — remove FK on column referencing `maintenance.work_order` because refunds are triggered by cancellations/IROPs, not directly by work orders; link through flight.cancellation or flight.irop_event instead.
**PRIORITY 9 — remove_fk: reservation.group_booking** — remove FK on column referencing `cargo.awb` because group passenger bookings should not reference air waybills; these are separate passenger vs. cargo domains.
**PRIORITY 10 — remove_fk: reservation.group_booking** — remove FK on column referencing `cargo.booking` because passenger group bookings have no business relationship to cargo bookings.
**PRIORITY 11 — rename_product: flight.status** — rename to `flight_status` because "status" is overly generic and ambiguous across domains; the qualified name clarifies it tracks flight operational status.
**PRIORITY 12 — rename_product: inventory.segment** — rename to `inventory_segment` because "segment" is ambiguous (conflicts with reservation.itinerary_segment concept) and needs domain qualification for clarity.
**PRIORITY 13 — rename_product: flight.plan** — rename to `flight_plan` because "plan" is overly generic and conflicts with finance.budget_plan naming pattern; needs domain-specific qualification.
**PRIORITY 14 — rename_product: maintenance.check** — rename to `maintenance_check` because "check" is a reserved word in many databases and overly generic without domain context.
**PRIORITY 15 — connect_table: airport.ground_handler** — add column `station_id` (BIGINT) with FK to `airport.station.station_id` — ground handlers operate at specific airport stations and this relationship is fundamental to ground operations management.
**PRIORITY 16 — connect_table: fleet.aircraft_type** — add column `manufacturer_name` as attribute and ensure no dangling FK; currently aircraft_type is a valid reference table with inbound FKs but verify it has no unresolved _id columns.
**PRIORITY 17 — connect_table: revenue.corporate_account** — add column `legal_entity_id` (BIGINT) with FK to `finance.legal_entity.legal_entity_id` — corporate accounts represent legal business entities that should link to the airline's legal entity registry for contract management.
**PRIORITY 18 — connect_table: revenue.agency** — add column `station_id` (BIGINT) with FK to `airport.station.station_id` — travel agencies operate at specific locations and BSP settlements require geographic attribution.
**PRIORITY 19 — connect_table: loyalty.tier_status** — add column `partner_program_id` (BIGINT) with FK to `loyalty.partner_program.partner_program_id` — tier status recognition across partner programs requires linking tiers to partnership agreements.
**PRIORITY 20 — connect_table: maintenance.airworthiness_directive** — add column `aircraft_type_id` (BIGINT) with FK to `fleet.aircraft_type.aircraft_type_id` — ADs are issued against specific aircraft type certificates and must reference the applicable type.
**PRIORITY 21 — connect_table: maintenance.service_bulletin** — add column `aircraft_type_id` (BIGINT) with FK to `fleet.aircraft_type.aircraft_type_id` — service bulletins are issued by manufacturers for specific aircraft types.
**PRIORITY 22 — connect_table: cargo.freight_forwarder** — add column `station_id` (BIGINT) with FK to `airport.station.station_id` — freight forwarders operate from specific airport stations for cargo acceptance and delivery.
**PRIORITY 23 — rename_attribute: crew.absence** — rename column `previous_absence_id` to `preceding_absence_id` because "previous" is ambiguous about whether it means the prior absence in a chain or a replaced absence; "preceding" clarifies sequential relationship.
**PRIORITY 24 — connect_table: flight.flight_leg** — add column `origin_station_id` (BIGINT) with FK to `airport.station.station_id` — flight legs must reference departure airport station for operational routing.
**PRIORITY 25 — connect_table: flight.flight_leg** — add column `destination_station_id` (BIGINT) with FK to `airport.station.station_id` — flight legs must reference arrival airport station for operational routing.
**PRIORITY 26 — connect_table: flight.scheduled_flight** — add column `origin_station_id` (BIGINT) with FK to `airport.station.station_id` — scheduled flights require origin airport reference for schedule publication.
**PRIORITY 27 — connect_table: flight.scheduled_flight** — add column `destination_station_id` (BIGINT) with FK to `airport.station.station_id` — scheduled flights require destination airport reference for schedule publication.
**PRIORITY 28 — connect_table: inventory.cabin_class** — add column `aircraft_type_id` (BIGINT) with FK to `fleet.aircraft_type.aircraft_type_id` — cabin class definitions vary by aircraft type and need this physical constraint reference.
**PRIORITY 29 — remove_fk: crew.flight_leg_assignment** — remove FK on column referencing `inventory.fare_class_bucket` because crew assignments to flight legs have no business relationship to fare class buckets; crew scheduling is independent of revenue management buckets.
**PRIORITY 30 — connect_table: cargo.shipper** — add column `station_id` (BIGINT) with FK to `airport.station.station_id` — shippers have primary origin stations for cargo acceptance operations.
**PRIORITY 31 — connect_table: route.city_pair** — add column `origin_station_id` (BIGINT) with FK to `airport.station.station_id` — city pairs currently reference station but should explicitly name origin vs destination to avoid ambiguity.
**PRIORITY 32 — rename_attribute: finance.legal_entity** — rename column `parent_entity_legal_entity_id` to `parent_legal_entity_id` because the current name is redundant (contains both "entity" and "legal_entity") and a cleaner self-ref already exists.
**PRIORITY 33 — rename_attribute: finance.legal_entity** — rename column `primary_ultimate_parent_entity_legal_entity_id` to `ultimate_parent_legal_entity_id` because the current name is excessively long and redundant.
**PRIORITY 34 — rename_attribute: flight.dispatch_release** — rename column `superseded_by_release_dispatch_release_id` to `superseded_by_dispatch_release_id` because "release" appears redundantly in the column name.
**PRIORITY 35 — rename_attribute: ancillary.category** — rename column `parent_category_ancillary_category_id` to `parent_category_id` because the self-reference name is redundantly verbose with domain and table name embedded.
**PRIORITY 36 — rename_attribute: passenger.apis_submission** — rename column `original_submission_apis_submission_id` to `original_apis_submission_id` because "submission" appears redundantly in the column name.
**PRIORITY 37 — connect_table: loyalty.miles_balance** — add column `tier_status_id` (BIGINT) with FK to `loyalty.tier_status.tier_status_id` — miles balance expiry and earning rates are tier-dependent and need this linkage for business rules.
**PRIORITY 38 — connect_table: loyalty.miles_transfer** — add column `recipient_ffp_member_id` (BIGINT) with FK to `loyalty.ffp_member.ffp_member_id` — miles transfers require both source and destination member references; currently only one member FK exists.
**PRIORITY 39 — connect_table: flight.oooi_event** — add column `station_id` (BIGINT) with FK to `airport.station.station_id` — OOOI events occur at specific airports and need station reference for OTP reporting.
**PRIORITY 40 — connect_table: flight.status** — add column `station_id` (BIGINT) with FK to `airport.station.station_id` — flight status updates originate from specific stations and need geographic attribution.
**PRIORITY 41 — connect_table: crew.roster_activity** — add column `duty_period_id` (BIGINT) with FK to `crew.duty_period.duty_period_id` — roster activities map to duty periods for FTL compliance tracking.
**PRIORITY 42 — connect_table: safety.occurrence** — add column `station_id` (BIGINT) with FK to `airport.station.station_id` — safety occurrences happen at specific locations and need direct station reference for geographic analysis.
**PRIORITY 43 — connect_table: safety.occurrence** — add column `flight_leg_id` (BIGINT) with FK to `flight.flight_leg.flight_leg_id` — most safety occurrences relate to specific flight legs and need this direct operational link.
**PRIORITY 44 — remove_fk: ancillary.baggage_allowance** — remove FK on column referencing `route.interline_agreement` because baggage allowance rules are defined by fare class and cabin, not by specific interline agreements; interline baggage rules should be derived through the ticketing path.
**PRIORITY 45 — connect_table: revenue.fare** — add column `route_id` (BIGINT) with FK to `route.route.route_id` — verify this exists; fares are fundamentally route-specific and this is the most critical pricing relationship (noted as existing but confirming completeness).
**PRIORITY 46 — connect_table: fleet.engine** — add column `aircraft_type_id` (BIGINT) with FK to `fleet.aircraft_type.aircraft_type_id` — engines are type-certified for specific aircraft types and need this reference for maintenance program applicability.
**PRIORITY 47 — connect_table: cargo.dangerous_goods** — add column `booking_id` (BIGINT) with FK to `cargo.booking.booking_id` — dangerous goods declarations must link to the cargo booking that initiated the shipment.
**PRIORITY 48 — connect_table: cargo.customs_entry** — add column `shipment_id` (BIGINT) with FK to `cargo.shipment.shipment_id` — customs entries are filed against specific shipments, not just AWBs.
**PRIORITY 49 — connect_table: maintenance.defect_report** — add column `release_id` (BIGINT) with FK to `maintenance.release.release_id` — defect reports that are rectified result in a maintenance release; this closes the defect lifecycle.
**PRIORITY 50 — connect_table: flight.weight_balance** — add column `cabin_configuration_id` (BIGINT) with FK to `fleet.cabin_configuration.cabin_configuration_id` — weight and balance calculations depend on the specific cabin configuration (seat count, galley positions).
**PRIORITY 51 — remove_fk: safety.occurrence** — remove FK on column referencing `route.seasonal_schedule` because occurrences relate to actual flights, not seasonal schedule entries; the link should be through flight_leg or scheduled_flight.
**PRIORITY 52 — connect_table: airport.baggage_scan** — add column `turnaround_id` (BIGINT) with FK to `airport.turnaround.turnaround_id` — baggage scans occur during turnaround operations and need this operational context.
**PRIORITY 53 — connect_table: crew.pairing** — add column `carrier_id` (BIGINT) with FK to `route.carrier.carrier_id` — crew pairings are built for a specific operating carrier, essential for codeshare crew assignment.
**PRIORITY 54 — connect_table: loyalty.award_booking** — add column `booking_passenger_id` (BIGINT) with FK to `reservation.booking_passenger.booking_passenger_id` — award bookings create passenger records that must link to the booking passenger entity.
**PRIORITY 55 — rename_attribute: safety.audit_finding** — rename column `previous_finding_audit_finding_id` to `preceding_audit_finding_id` because the current name redundantly includes "finding" twice.
**PRIORITY 56 — connect_table: revenue.tax_fee** — add column `route_id` (BIGINT) with FK to `route.route.route_id` — taxes and fees are route-specific (departure/arrival country taxes) and need route attribution.
**PRIORITY 57 — connect_table: revenue.fare_rule** — add column `route_id` (BIGINT) with FK to `route.route.route_id` — fare rules (advance purchase, min stay, routing) are route-dependent and need this geographic constraint.
**PRIORITY 58 — connect_table: inventory.waitlist_entry** — add column `itinerary_segment_id` (BIGINT) with FK to `reservation.itinerary_segment.itinerary_segment_id` — waitlist entries are for specific itinerary segments, not just inventory-level.
**PRIORITY 59 — connect_table: flight.irop_event** — add column `flight_leg_id` (BIGINT) with FK to `flight.flight_leg.flight_leg_id` — IROP events are triggered by specific flight legs (the impacted flight); currently only has indirect link through irop_flight_impact.
**PRIORITY 60 — connect_table: cargo.uld_content** — add column `uld_id` (BIGINT) with FK to `cargo.uld.uld_id` — ULD contents must directly reference the ULD they are loaded into, not just through uld_movement.

Other known issues from static analysis (5):
  - Attribute 'bundle_tier' in ancillary.bundle has redundant product prefix 'bundle_'. Rename to 'tier'
  - 8 attribute(s) contain banned boilerplate phrases (Fortune N, multinational, enterprise-wide, cross-company, group reporting). Clean descriptions.
  - 108 attribute(s) match person-data patterns but lack pii_ tags
  - 12 non-PK attributes redundantly prefixed with their product name
  - 1 attribute(s) have pipe-enum value_regex with >8 values — consider creating a reference product instead

Deterministic score: 93/100 (LLM assessment: 62/100)
